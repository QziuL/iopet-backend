services:
  api:
    container_name: iopet-api

    build:
      context: ./api
      dockerfile: Dockerfile

    ports:
      - "8080:8080"

    env_file:
      - ./.env

    environment:
      SERVER_PORT: 8080

    restart: unless-stopped